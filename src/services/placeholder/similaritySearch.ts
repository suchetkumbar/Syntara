/**
 * Embedding-Based Similarity Search (Placeholder)
 *
 * Future: Find similar prompts using vector embeddings
 * - Semantic search across prompt library
 * - Cluster related prompts
 * - Suggest similar existing prompts when creating new ones
 */

export function findSimilarPrompts(_query: string, _limit: number): string[] {
  throw new Error("Not implemented — similarity search is a future feature.");
}
