/**
 * Prompt Debugger (Placeholder)
 *
 * Future: Analyze why a prompt produces unexpected results
 * - Identify ambiguous sections
 * - Detect conflicting instructions
 * - Suggest fixes
 */

export function debugPrompt(_prompt: string): string[] {
  throw new Error("Not implemented — prompt debugger is a future feature.");
}
